pipeline {
    agent { 
		docker { 
			image 'maven:latest' 
		}
	}
    stages {
        stage('tests') {
		configFileProvider(
			[configFile(fileId: 'settings.xml',
				variable: 'MAVEN_SETTINGS')]) {
			sh 'mvn -s $MAVEN_SETTINGS clean install'
		}
        }
    }
}
