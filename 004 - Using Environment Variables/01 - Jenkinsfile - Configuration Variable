pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
		parameters { string(name: 'NOTIFICATION_EMAIL', defaultValue: 'teste@email.com', description: '') }
	}
    stages {
        stage('build') {
            steps {
                echo "$NOTIFICATION_EMAIL"
            }
        }
    }
}
