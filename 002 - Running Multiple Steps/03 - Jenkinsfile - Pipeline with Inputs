pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            input {
                message "Name?"
                parameters {
                    string(name: 'NAME', defaultValue: '', description: '')
                }
		input {
                message "Age?"
                parameters {
                    string(name: 'AGE', defaultValue: '', description: '')
                }
            steps {
                echo "$NAME"
                echo "$AGE"
            }
        }
    }
}
